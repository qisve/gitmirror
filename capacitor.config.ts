import type { CapacitorConfig } from '@capacitor/cli'
const config: CapacitorConfig = {
  appId: 'com.gitmirror.app',
  appName: 'GitMirror',
  webDir: 'dist',
  androidScheme: 'https',
}
export default config
